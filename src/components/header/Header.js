import React from "react";
import "./Header.scss";
import { Container } from "react-bootstrap";

const Header = () => {
    return (
        <header className="pageHeader py-4">
            <Container>Profile</Container>
        </header>
    );
};

export default Header;
